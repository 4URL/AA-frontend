import React from 'react';

function LoginSideBox() {
  const appTitle = 'Awesome Animal';

  return (
    <div className="login-side-box">
      <div className="login-title"> {appTitle} </div>
      <div className="login-side-image"></div>
    </div>
  );
}

export default LoginSideBox;
